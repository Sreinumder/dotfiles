--┌─────────────────────────────────────────┐
--│changes naming convention-like camel case│
--│key bind prefix: cr in visual mode       │
--└─────────────────────────────────────────┘
return {
	"gregorias/coerce.nvim",
	tag = "v0.3",
	-- keys = {
	-- 	{ "cr", mode = { "v", "n" } },
	-- },
  opts = {},
	-- config = true,
}
